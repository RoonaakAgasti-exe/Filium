# The eval scripts import the ingestion pipeline (answer_query.py) by
# adding ingestion/ to sys.path themselves, so `python eval/<script>.py`
# from the repo root works with no PYTHONPATH set. They do need a
# populated database and a real OPENAI_API_KEY.
openai==1.54.0
psycopg2-binary==2.9.9
python-dotenv==1.0.1
requests==2.32.3
