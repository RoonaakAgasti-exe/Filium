# Test-only dependencies. Kept separate from backend/requirements.txt so
# the runtime image doesn't ship a test runner.
pytest==8.3.3
pytest-cov==5.0.0

# fastapi.testclient is a thin wrapper over httpx — without it,
# `from fastapi.testclient import TestClient` raises at import and every
# test in test_routers.py errors during collection. It happens to arrive
# as a transitive dependency of `openai`, which is exactly the kind of
# accident that breaks the day someone trims that dependency.
httpx==0.28.1
